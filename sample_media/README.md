# AutoSmokeGuard sample media

Test fixtures for the analysis pipeline. Drop any of these into the uploader,
or run `python -m mlcore.selftest` to analyse all of them at once.

> **The vehicles are real; the smoke is not.**
> Every file below is derived from a real photograph (the COCO128 sample set,
> which ships with Ultralytics) so the YOLO vehicle detector has genuine
> vehicles to find. The exhaust plumes are **synthesised** by the same
> fractal-Brownian-motion plume model used to build the training set
> (`mlcore/training/synth_dataset.py`). No real vehicle in these files was
> actually emitting smoke. They exist to demonstrate and regression-test the
> pipeline, not to prove real-world accuracy.

| File | What it is | Expected analysis outcome |
| --- | --- | --- |
| `sample_truck_smoking.mp4` | ~6 s, 640x480, 20 fps. A slow pan-and-zoom over a real street photo of trucks, with an animated exhaust plume composited at the rear of the largest vehicle. | Vehicles detected on most sampled frames; **at least one smoke region**, typically `moderate`/`high` severity. |
| `sample_car_clean.mp4` | ~6 s, 640x480, 20 fps. Same construction over a real street photo with a car, **no smoke added**. | Vehicles detected; **no smoke regions** (a stray low-severity hit is tolerated, see the self-test tolerance). |
| `sample_bus_smoking.jpg` | The real `bus.jpg` street photo with a synthesised plume composited at the rear of the bus. | One bus detected; **at least one smoke region**. |
| `sample_street_clean.jpg` | An unmodified real COCO128 street photo. Nothing has been added or removed. | Vehicles detected; **no smoke regions**. |

## Provenance

| Output | Source photograph |
| --- | --- |
| `sample_truck_smoking.mp4` | `000000000257.jpg` |
| `sample_car_clean.mp4` | `000000000094.jpg` |
| `sample_bus_smoking.jpg` | `bus.jpg` |
| `sample_street_clean.jpg` | `000000000471.jpg` |

## Regenerating

```bash
python tools/make_samples.py            # rebuild everything
python tools/make_samples.py --seed 7   # different plume shapes
```

Videos are written with the `mp4v` fourcc. The generator tries `mp4v`,
then `avc1`, then falls back to `MJPG` in an `.avi` container, verifying after
each attempt that the file reopens with the expected frame count.

_Generated by `tools/make_samples.py` on 2026-09-22._
