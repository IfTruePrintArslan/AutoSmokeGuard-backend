"""
Tests for the shared utilities live in the project-level ``tests/`` package
(``tests/test_foundation.py``) so the whole foundation layer is covered by one
run of ``pytest``; ``pytest.ini`` points ``testpaths`` there.
"""
