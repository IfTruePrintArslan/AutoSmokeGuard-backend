"""
Project-level test suite.

All tests live here rather than in per-app ``tests.py`` modules so that the
foundation layer, which spans every app, can be exercised in one place and so
``pytest.ini`` needs a single ``testpaths`` entry.
"""
